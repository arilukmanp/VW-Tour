/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.traveldailymedia.com", "images.pexels.com"],
  },
};

module.exports = nextConfig;
